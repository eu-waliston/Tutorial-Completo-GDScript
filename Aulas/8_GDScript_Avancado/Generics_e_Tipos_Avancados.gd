# Arrays tipados
var int_array: Array[int] = [1, 2, 3]
var string_array: Array[String] = ["a", "b", "c"]

# Dicionários tipados
var player_data: Dictionary = {
    "name" : "player",
    "score" : 1000,
    "level" : 5
}

# Enums
enum Element {FIRE, WATER, EARTH, AIR}
var current_element: Element = Element.FIRE

# Sets
# var unique_items := Set([1, 2, 3, 2, 1])  # Resultado: {1, 2, 3}
